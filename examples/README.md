

Note that the example.qmd file is, at the time of writing, the same as the template.qmd at the root of this project.

However, the example.qmd does not see the extension for the slides so can't be executed in this folder (or possibly at all unless under the name template.qmd? Not sure.)

In any case, the example.qmd is intended to be a more built-out example in the future.

TODO: replace the template.html with an image of what the RevealJS output looks like.